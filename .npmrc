# Several UI libs (Excalidraw, Mafs, etc.) declare peer ranges that lag React 19.
# Pin legacy peer resolution so `npm install` is reproducible and doesn't silently
# downgrade packages (which broke the Excalidraw CSS export). Use `npm ci` for exact installs.
legacy-peer-deps=true
